name: Call CI
on: pull_request

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  call:
    name: CI
    permissions:
      contents: read # needed for reusable workflow
    uses: pHo9UBenaA/gh-action-templates/.github/workflows/pnpm-biome-ci.yml@19edb5410545784a60afe55689770f09407c704f # master
