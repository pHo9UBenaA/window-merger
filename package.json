{
	"name": "window-merger",
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"build": "bun run ./bun.build.js",
		"format": "biome format --write .",
		"lint": "biome lint --write .",
		"check": "biome check --write .",
		"zip": "zip -vr dist.zip dist -x \"*.DS_Store\"",
		"test:unit": "vitest"
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"@types/chrome": "^0.0.293",
		"glob": "^10.3.3",
		"typescript": "^5.3.2",
		"vitest": "^2.1.8"
	}
}
